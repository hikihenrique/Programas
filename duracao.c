#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]){

	int hin, min, hf, mf, durh, durm;
	scanf("%d %d %d %d", &ain, &bin, &hf, %mf);
	if (hf > hin && mf > min){
		durh = hf - hin;
		durm = mf - min;
		printf("O JOGO DUROU %d HORA(S) E %d MINUTO(S)\n", dur);
	}
	if (hf < hin){
		hf += 24;
		durh = hf - hin;
		if (mf >)
		printf("O JOGO DUROU %d HORA(S) E %d MINUTO(S)\n", dur);

	}


	return 0;
}